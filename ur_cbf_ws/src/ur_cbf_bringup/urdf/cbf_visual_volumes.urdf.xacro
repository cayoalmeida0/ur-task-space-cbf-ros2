<?xml version="1.0"?>
<!--
  Aproximacoes geometricas visuais para inspecao das futuras CBFs.

  Estes links nao possuem collision nem inertial: aparecem no RViz e no Gazebo,
  acompanham os elos por juntas fixas e nao alteram a fisica da simulacao.
-->
<robot xmlns:xacro="http://wiki.ros.org/xacro">
  <xacro:macro name="cbf_sphere_visual"
               params="name parent xyz radius rgba gazebo_visible">
    <link name="${name}_cbf_volume">
      <visual name="${name}_cbf_visual">
        <origin xyz="${xyz}" rpy="0 0 0"/>
        <geometry>
          <sphere radius="${radius}"/>
        </geometry>
        <material name="${name}_cbf_material">
          <color rgba="${rgba}"/>
        </material>
      </visual>
    </link>
    <joint name="${name}_cbf_volume_joint" type="fixed">
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <parent link="${parent}"/>
      <child link="${name}_cbf_volume"/>
    </joint>
    <!-- RViz ignora a extensao gazebo; visibility_flags=0 oculta somente no SDF. -->
    <xacro:unless value="${gazebo_visible}">
      <gazebo reference="${name}_cbf_volume">
        <visual>
          <visibility_flags>0</visibility_flags>
        </visual>
      </gazebo>
    </xacro:unless>
  </xacro:macro>

  <!-- Uma capsula e formada por um cilindro e duas esferas, todos visuais. -->
  <xacro:macro name="cbf_capsule_visual"
               params="name parent center rpy cap_a cap_b radius length rgba gazebo_visible">
    <link name="${name}_cbf_volume">
      <visual name="${name}_cbf_cylinder">
        <origin xyz="${center}" rpy="${rpy}"/>
        <geometry>
          <cylinder radius="${radius}" length="${length}"/>
        </geometry>
        <material name="${name}_cbf_material">
          <color rgba="${rgba}"/>
        </material>
      </visual>
      <visual name="${name}_cbf_cap_a">
        <origin xyz="${cap_a}" rpy="0 0 0"/>
        <geometry>
          <sphere radius="${radius}"/>
        </geometry>
        <material name="${name}_cbf_material"/>
      </visual>
      <visual name="${name}_cbf_cap_b">
        <origin xyz="${cap_b}" rpy="0 0 0"/>
        <geometry>
          <sphere radius="${radius}"/>
        </geometry>
        <material name="${name}_cbf_material"/>
      </visual>
    </link>
    <joint name="${name}_cbf_volume_joint" type="fixed">
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <parent link="${parent}"/>
      <child link="${name}_cbf_volume"/>
    </joint>
    <xacro:unless value="${gazebo_visible}">
      <gazebo reference="${name}_cbf_volume">
        <visual>
          <visibility_flags>0</visibility_flags>
        </visual>
      </gazebo>
    </xacro:unless>
  </xacro:macro>

  <xacro:macro name="ur3e_cbf_visual_volumes" params="prefix gazebo_visible">
    <xacro:property name="arm_color" value="0.00 0.75 1.00 0.25"/>
    <!--
      Offsets fisicos da descricao oficial Jazzy do UR3e. Os eixos DH passam
      pelas juntas, enquanto os corpos do braco e antebraco sao deslocados em z
      nos respectivos frames de elo.
    -->
    <xacro:property name="shoulder_body_offset" value="0.120"/>
    <xacro:property name="elbow_body_offset" value="0.027"/>
    <xacro:property name="wrist_1_joint_offset" value="0.13105"/>

    <xacro:cbf_sphere_visual
      name="${prefix}base" parent="${prefix}base_link"
      xyz="0 0 0.075" radius="0.110" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_sphere_visual
      name="${prefix}shoulder" parent="${prefix}shoulder_link"
      xyz="0 0 0" radius="0.090" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_capsule_visual
      name="${prefix}upper_arm" parent="${prefix}upper_arm_link"
      center="-0.121775 0 ${shoulder_body_offset}" rpy="0 1.57079632679 0"
      cap_a="0 0 ${shoulder_body_offset}"
      cap_b="-0.24355 0 ${shoulder_body_offset}"
      radius="0.075" length="0.24355" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_sphere_visual
      name="${prefix}elbow" parent="${prefix}forearm_link"
      xyz="0 0 0" radius="0.075" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_capsule_visual
      name="${prefix}forearm" parent="${prefix}forearm_link"
      center="-0.1066 0 ${elbow_body_offset}" rpy="0 1.57079632679 0"
      cap_a="0 0 ${elbow_body_offset}"
      cap_b="-0.2132 0 ${elbow_body_offset}"
      radius="0.065" length="0.2132" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <!--
      Completa o offset distal do antebraco ate o frame posterior a junta 4.
      Comprimento: 0.13105 - 0.027 = 0.10405 m.
    -->
    <xacro:cbf_capsule_visual
      name="${prefix}wrist_1_connector" parent="${prefix}forearm_link"
      center="-0.2132 0 0.079025" rpy="0 0 0"
      cap_a="-0.2132 0 ${elbow_body_offset}"
      cap_b="-0.2132 0 ${wrist_1_joint_offset}"
      radius="0.060" length="0.10405" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_sphere_visual
      name="${prefix}wrist_1" parent="${prefix}wrist_1_link"
      xyz="0 0 0" radius="0.060" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_sphere_visual
      name="${prefix}wrist_2" parent="${prefix}wrist_2_link"
      xyz="0 0 0" radius="0.058" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
    <xacro:cbf_sphere_visual
      name="${prefix}wrist_3" parent="${prefix}wrist_3_link"
      xyz="0 0 0" radius="0.058" rgba="${arm_color}"
      gazebo_visible="${gazebo_visible}"/>
  </xacro:macro>

  <xacro:macro name="rg2_cbf_visual_volume" params="prefix gazebo_visible">
    <!-- Uma unica capsula cobre corpo, dedos e o TCP nominal da RG2. -->
    <xacro:cbf_capsule_visual
      name="${prefix}rg2" parent="${prefix}onrobot_base_link"
      center="0 0 0.110" rpy="0 0 0"
      cap_a="0 0 0.055" cap_b="0 0 0.165"
      radius="0.090" length="0.110" rgba="1.00 0.45 0.00 0.30"
      gazebo_visible="${gazebo_visible}"/>
  </xacro:macro>
</robot>
